node_modules
.vercel
frontend/.vercel
.next
dist
.env
.env.local
.env.*.local
frontend/.env.build.meta
frontend/.env.production.local
frontend/public/build-id.txt
.DS_Store
coverage
.npm-cache
var/eticket-pdf-cache

# Local pg_dump outputs (scripts/backup-postgres.sh)
backups/*.dump

# Encrypted local backup runs (never commit)
backend/var/backups/
