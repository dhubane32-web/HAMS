# Copy to .env.local — must match backend listen port (see backend/.env PORT).
# Next.js dev is usually http://localhost:3000; backend FRONTEND_URL should include that origin in production.
NEXT_PUBLIC_API_URL=http://localhost:5013
# Default admin (after database/auth_hawana_admin.sql): admin@hawanaairways.com / Admin12345!
